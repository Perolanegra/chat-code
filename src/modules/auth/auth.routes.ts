import { Routes } from '@angular/router';
import { AuthLoginComponent } from './components/ui/login/login.component';

export const AUTH_ROUTES: Routes = [
  {
    path: '',
    component: AuthLoginComponent,
  },
];
